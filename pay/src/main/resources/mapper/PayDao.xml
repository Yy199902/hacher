<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pay.pay.dao.PayDao">
    <insert id="add">
        insert into event values(#{event.id}, #{event.type})
    </insert>
    <update id="updateByType">
        update event set type = 3 where id = #{i}
    </update>
    <update id="updateTr">
        update tr set type = 3 where id = #{i}
    </update>
</mapper>